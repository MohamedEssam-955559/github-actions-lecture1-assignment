name: PR Path Filter Workflow

on:
    pull_request:
        branches:
            - main
        paths:
            - "src/**"

jobs:
    pr-path-check:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Confirm PR Path Trigger
              run: |
                  echo "Pull Request affects files in src/ directory"
